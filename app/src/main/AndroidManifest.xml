<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oods.produk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Pembeli.Dashboard"
            android:label="@string/title_activity_dashboard"></activity>
        <activity android:name=".Pembeli.Register" />
        <activity android:name=".Pembeli.Login" />
        <activity android:name=".Pembeli.Splashscreen" />
        <activity android:name=".Pembeli.WelcomeActivity" />
        <activity android:name=".Pembeli.Intro" />
        <activity android:name=".Admin.ImagesActivity" />
        <activity android:name=".Admin.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ImageActivity" />
    </application>

</manifest>